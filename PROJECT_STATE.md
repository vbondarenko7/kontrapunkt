# salon-landing

## Статус

Содержимое локального лендинга возвращено побайтно к версии до изменений `ec64021`: исходные тексты,
служебные `[ЗАПОЛНИТЬ]`, цена `2 300 ₽` и отключённые кнопки «Купить билет». Счётчик Яндекс Метрики
`111267605` сохранён. GitHub Pages не обновлялся.

## Структура

- `Джаз вечер. /Лендинг версия C/app/page.tsx` — исходная согласованная версия страницы.
- `app/YandexMetrika.tsx` — сохранённый счётчик `111267605`, Webvisor и параметры из присланного кода.
- `app/analytics.ts` — сохранённый вспомогательный код цели `telegram_click`; текущая страница его не вызывает.
- `tests/rendered-html.test.mjs` — 8 проверок исходной страницы, Метрики и waitlist API.

## Ключевые решения

- По команде пользователя все изменения содержимого сайта и Telegram CTA отменены.
- Яндекс Метрика не откатывалась и не изменялась.
- Публикация не выполнялась; живой GitHub Pages оставался прежним всё время.
- Новые изменения сайта не выполнять без отдельного прямого запроса.

## Что сделано

- `app/page.tsx` совпадает с версией до `ec64021`, git blob `0e290ef3b3c980297b10a0b156a01b6fd4093127`.
- Тест рендера возвращён к ожиданиям исходной страницы.
- `npm run lint`, сборка и 8 тестов проходят.
- Откат содержимого зафиксирован коммитом `8364011`.

## Что дальше

Не менять и не публиковать сайт без отдельного прямого запроса пользователя.

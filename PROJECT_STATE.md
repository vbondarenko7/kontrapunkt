# salon-landing

## Статус

Локальная prelaunch-версия готова: вместо нерабочего билетного шага одна микроконверсия — переход в Telegram.
Метрика и Вебвизор подготовлены, но ждут номер счётчика. GitHub Pages не обновлялся.

## Структура

- `Джаз вечер. /Лендинг версия C/app/page.tsx` — актуальная страница и Telegram CTA.
- `app/YandexMetrika.tsx` внутри лендинга — `tag.js`, карта кликов, ссылки, точный отказ и Вебвизор.
- `app/analytics.ts` — цель `telegram_click` и параметр `placement`.
- `tests/rendered-html.test.mjs` — 8 проверок рендера, аналитики и сохранённого waitlist API.
- `../Маркетинг/проекты/джазовые-вечера/FIRST_TEST_SYSTEM_V1.md` — аналитическая схема и UTM.

## Ключевые решения

- До билетной ссылки все главные CTA ведут в `https://t.me/VadimBond7`.
- Цена и служебные `[ЗАПОЛНИТЬ]` не показываются; неизвестные факты обозначены как будущий анонс.
- Код Метрики не рендерится без числового `NEXT_PUBLIC_YANDEX_METRIKA_ID`.
- Публикация GitHub Pages только по отдельному подтверждению.

## Что сделано

- Заменены отключённые билетные CTA и удалена неподтверждённая цена.
- Добавлены цель `telegram_click` и три места CTA.
- Добавлены Метрика и Вебвизор с безопасной активацией по номеру счётчика.
- `npm run lint`, `npm test` и сборка проходят; 8 тестов зелёные.
- Проверены ширины 390, 768 и 1440 px: переполнения нет, CTA видны.

## Что дальше

1. Получить номер счётчика, создать цель `telegram_click` и проверить `_ym_debug=2`.
2. По отдельному подтверждению пересобрать статический экспорт и обновить GitHub Pages.
3. После появления даты, цены, состава и билетной ссылки вернуть прямую покупку как главный CTA.
